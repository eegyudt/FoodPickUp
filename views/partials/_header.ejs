<header>
  <nav class="navbar">
    <div class="pizza">
      <h1>Pizzaholic Restaurant</h1>
    </div>

    <div class="loginLink">
      <% if(user.id){ %>
        <div class="rigth-nav">
          <h6 class="loginPromt">Hello <%= user.name %> </h6>
          <input class="logout_button" type="button" onclick="location.href='/logout';" value="Log out" />
        </div>
    </div>
    <% } else{ %>
      <div class="links">
        <a class="menu" href="/menu">Menu</a>
        <a class="login" href="/login">Login</a>
        <a class="reg" href="/register">Register</a>
      </div>
      <% } %>
  </nav>
</header>