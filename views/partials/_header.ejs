<header>
  <nav class="navbar">
    <div class="pizza">
      <h1>Pizzaholic Restaurant</h1>
    </div>

    <div class="loginLink">
      <div>
        <% if(user.id){ %>

          <div>
            <div class="rigth-nav">
              <h5 class="hello">Hello <%= user.name %></h5>


              <input class="logout_button" type="button" onclick="location.href='/logout';" value="Log out" />
            </div>
          </div>
          <% } else{ %>
            <div class="links">
              <a class="menuNav" href="/menu">Menu</a>
              <a class="loginNav" href="/login">Login</a>
              <a class="regNav" href="/register">Register</a>
            </div>
            <% } %>
      </div>
    </div>
    <% } else{ %>
      <div class="links">
        <a class="menu" href="/menu">Menu</a>
        <a class="login" href="/login">Login</a>
        <a class="reg" href="/register">Register</a>
      </div>
      <% } %>
  </nav>
</header>